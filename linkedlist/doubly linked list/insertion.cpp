#include<bits/stdc++.h>
using namespace std;

class Node
{
    int data;
    Node *prev;
    Node *next;
    Node(int val)
    {
        data = val;
        prev = NULL;
        next = NULL;
    }
};

void push(Node *head, int data)
{
    Node *nptr = new Node(data);
    
}
int main()
{
    
    return 0;
}